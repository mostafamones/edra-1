import { SettingsTabProps } from "@/components/helpers/settings"

export function IntegerationSettings({ disabled, academyId }: SettingsTabProps) {
  return (
    <div>
      Integeration Settings
    </div>
  )
}